# -----------------------------------------------------------------------------
# GCP Configuration
# -----------------------------------------------------------------------------

project_id = "hc-00d1b45fd9b24a22b61e9b206df"
region     = "us-central1"
zone       = "us-central1-a"

# -----------------------------------------------------------------------------
# Vault Configuration
# -----------------------------------------------------------------------------

vault_addr     = "https://vault-cluster-public-vault-b221167d.82afa726.z1.hashicorp.cloud:8200"
vault_ssh_path = "ssh"
vault_ssh_role = "ssh-role"

# -----------------------------------------------------------------------------
# AAP Configuration
# -----------------------------------------------------------------------------

aap_host            = "https://aap.pablo-diaz.sbx.hashidemos.io/"
aap_token           = "60DcjVGjz2ooE9sSDXlyRKz8iY0tUw"  # Consider using env var: TF_VAR_aap_token
aap_job_template_id = 30                     # ID of your job template
aap_organization_id = 1

# -----------------------------------------------------------------------------
# VM Configuration
# -----------------------------------------------------------------------------

vm_name      = "vault-ssh-demo"
machine_type = "e2-medium"
ssh_user     = "rhel"
os_image     = "rhel-cloud/rhel-9"

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------

network_name = "vault-ssh-demo-network"
subnet_cidr  = "10.0.1.0/24"

# IMPORTANT: Restrict this in production!
allowed_ssh_ranges = [
  "YOUR_AAP_CONTROLLER_IP/32",  # AAP controller IP
  "YOUR_OFFICE_IP/32",          # Your office IP for debugging
]

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

streamlit_port = 8501
